<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cart">
	<!-- 장바구니에 담기 -->
	<insert id="insert" parameterType="com.acorn.project.cart.dto.CartDto">
		insert into cart
		(cart_id, id, productId, amount)
		values
		(seq_cart.nextval, #{id}, #{productId}, #{amount})
	</insert>
</mapper>