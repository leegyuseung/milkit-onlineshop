<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">
	<insert id="insert" parameterType="productDto">
		INSERT INTO product
		(productId, productname, price, stock, productcate, regdate)
		VALUES(#{productId }, #{productname }, #{price }, #{stock }, #{productcate }, SYSDATE)
	</insert>
	
	<select id="getList" resultType="productDto">
		SELECT productId, productname, price, stock, productcate, regdate
		FROM product
		ORDER BY regdate ASC
	</select>
</mapper>






